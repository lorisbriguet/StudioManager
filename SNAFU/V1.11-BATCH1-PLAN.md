# V1.11 Batch 1 — Bulk PDF export, recurring UX, time-entries editor, task-status filtering

User-requested items (2026-07-29). All follow DESIGN-SYSTEM.md, i18n EN+FR (accent-free FR), preserve phase-1/2 wiring, targeted tests + tsc per task, full sweep at the end.

**Locked decisions:**
- "Monthly invoices" = recurring templates exist but are hidden/clunky → improve discoverability + management, no new billing engine.
- Due/overdue/task lists hide tasks of **cancelled and completed** projects; on-hold stays visible.

## Package A — InvoicesPage + QuotesPage (one implementer)

### A1: Bulk PDF export
- Add an "Export PDFs" action to the BulkActionBar on InvoicesPage and QuotesPage (icon: Download 14).
- Flow: folder picker (dialog `open({directory:true})` like the backup-path picker; session fs-scope grant needs `recursive:true` — mirror SettingsPage's picker comment), then for each selected doc generate the PDF via the existing per-doc generation path (reuse the exact pipeline handleDownloadPdf uses — read it; refactor its inner generate-to-bytes into a reusable function rather than duplicating) and write `<Reference>_<Client>.pdf` (mirror existing filename sanitization) into the folder.
- Busy: reuse the loading-toast pattern (`generating_pdf`-style, with progress "N/M"); double-fire guard via the existing busyPdfIds Set; per-doc failures collected → one summary toast (i18n keys `bulk_export_done` "{n} PDFs exported" / `bulk_export_partial` with {n}/{total}); cancel of the folder dialog = clean no-op.
- Drafts: exporting a DRAFT-referenced invoice PDF is allowed (matches single export's warning-then-proceed behavior — read it and mirror; if single export warns per doc, bulk warns ONCE for the whole selection if any draft is included).

### A2: Recurring discoverability + management
- Read the current recurring UX first (InvoicesPage "Recurring" toggle + templates section, useRecurring hooks, how a template is created today — find the actual entry point).
- Add: context-menu action "Make recurring…" on non-draft invoice rows (opens a small Modal: frequency select (monthly/quarterly/biannual/annual) + next-due date defaulting to invoice_date + 1 period via advanceDate from src/utils/recurringDates.ts) → creates the template via the existing create mutation. If an equivalent creation flow already exists somewhere, keep one canonical flow and make the context-menu item open it.
- Templates section improvements (whatever exists): each template row must show base invoice reference/client/amount, frequency, next_due, active toggle, and actions (pause/resume, delete with confirm). Add whatever of these is missing; keep the existing section layout otherwise.
- Discoverability: when the Recurring header toggle is off and templates exist, show the count chip (exists per phase-2 code — verify) — plus an empty-state CTA in the templates section pointing at "right-click an invoice → Make recurring" (i18n).

## Package B — SettingsPage time-entries editor (one implementer)

### B1: Widen the editing UI
- Read TimeEntriesManager in SettingsPage. The editing affordance is too narrow (user report). Fix per what's found: widen the edit modal to Modal size lg / widen inline-edit inputs (date, duration, project/task selects, description) so content fits comfortably at typical values; description gets flex-1. Follow the DS input patterns; no functional changes. Before/after note in report.

## Package C — Task visibility by project status (one implementer)

### C1: Hide cancelled+completed projects' tasks
- Find every consumer surfacing tasks by due date or listing tasks across projects: dashboard widgets (overdue tasks, upcoming deadlines, today's schedule, planned-vs-actual, weekly trend — read widgets.tsx + the queries they call in src/db/queries/tasks.ts), TasksPage (its `tasks` source + grouping), calendar task display (CalendarPage builds events from tasks — cancelled-project tasks should not appear there either; judge completed the same), notifications/overdue task checks if any.
- Fix at the QUERY level where possible (JOIN projects, `p.status NOT IN ('cancelled','completed')`) so all consumers inherit it; where a consumer intentionally shows all tasks (ProjectDetailContent inside a cancelled project — KEEP showing its own tasks there), leave untouched. Enumerate every consumer → filtered/left + why.
- TasksPage: its project grouping should skip cancelled/completed projects entirely (their groups disappear). Verify EmptyState truly-empty semantics still use the raw (now-filtered) source sensibly — judge and document.
- Tests: extend an existing query-level test file if the mock allows asserting the new WHERE/JOIN (cheap statement-log assertion like invoiceDelete.test.ts does); otherwise document manual steps.

## Final: verification sweep
- tsc, full vitest, lint (expect 0), EN/FR parity, then append a short "V1.11 batch 1" section to SNAFU/AUDIT-FIXES-TEST-PLAN.md (bulk export incl. cancel/partial-failure, make-recurring flow, template management, widened editor, cancelled/completed task filtering incl. calendar).
